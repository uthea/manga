apiVersion: v1
kind: Service
metadata:
  name: {{ include "manga-tracker.fullname" . }}
  labels:
    {{- include "manga-tracker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "manga-tracker.selectorLabels" . | nindent 4 }}
